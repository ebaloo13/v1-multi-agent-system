# Documentation Archive

This folder keeps historical docs that are no longer current product guidance.

Archived docs are preserved for reference only. Use the current guidance docs at
the repository root and under `docs/product/`, `docs/architecture/`, or
`docs/ops/` before relying on archived material.
